// Web stub for @react-native/assets-registry (a Flow-typed RN internal that
// react-native-svg pulls in). The docs preview never loads native image assets,
// so these no-ops are enough to keep the web bundle Flow-free.
export function registerAsset() {
  return 0
}
export function getAssetByID() {
  return null
}
